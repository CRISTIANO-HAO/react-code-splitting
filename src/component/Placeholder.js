import * as React from 'react'

export default class Placeholder extends React.Component {
  render() {
    return <h3 style={{textAlign: 'center', marginTop: 30, color: 'red'}} >the component is loading...</h3>
  }
}